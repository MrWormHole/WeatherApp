{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "Weather app that fetches json data from google api and dark sky api",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Talha Altınel",
  "license": "ISC",
  "dependencies": {
    "request": "^2.73.0",
    "yargs": "^4.8.1"
  }
}
